"use client";import {useState} from "react";import {useRouter} from "next/navigation";
export function SetupForm(){const [error,setError]=useState(""),[busy,setBusy]=useState(false);const router=useRouter();return <form onSubmit={async e=>{e.preventDefault();const f=new FormData(e.currentTarget);if(f.get("password")!==f.get("confirm")){setError("Kata sandi tidak sama.");return;}setBusy(true);try{const r=await fetch("/api/setup",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:f.get("name"),email:f.get("email"),password:f.get("password")})});const d=await r.json();if(!r.ok)throw Error(d.error);router.replace("/login");router.refresh();}catch(e){setError(e instanceof Error?e.message:"Gagal membuat akun.");}finally{setBusy(false);}}}><label className="form-field">Nama pengelola<input name="name" required maxLength={120} autoComplete="name"/></label><label className="form-field">Email<input name="email" type="email" required maxLength={254} autoComplete="username"/></label><label className="form-field">Kata sandi<input name="password" type="password" minLength={14} maxLength={128} required autoComplete="new-password"/></label><label className="form-field">Ulangi kata sandi<input name="confirm" type="password" minLength={14} maxLength={128} required autoComplete="new-password"/></label><p>Minimal 14 karakter. Kata sandi tidak dicatat atau ditampilkan.</p><p role="alert">{error}</p><button className="button" disabled={busy}>{busy?"Membuat akun…":"Buat akun admin"}</button></form>;}
